version: 2
jobs:
  test:
      docker:
        - image: circleci/python:3.6
      steps:
        - checkout
        - run: pip install tox
        - run: tox -- -k "not test_examples_experiment" -v 
workflows:
  version: 2
  build:
    jobs:
      - test
